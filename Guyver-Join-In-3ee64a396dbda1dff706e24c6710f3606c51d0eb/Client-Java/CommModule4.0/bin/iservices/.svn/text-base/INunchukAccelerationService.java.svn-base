package iservices;

import services.NunchukAccelerationServiceEvent;

import control.IListenerCommModule;

/**
 * Must be implemented by any class which wants to receive the events from a Nonin
 * @author Santiago Hors Fraile
 */
public interface INunchukAccelerationService extends IListenerCommModule  {
	
	/**
	 * Must be implemented to manage the NunchukAccelerationServiceEvent received.
	 * @param se This is the NunchukAccelerationServiceEvent received.
	 */
	void nunchukAccelerationReceived(NunchukAccelerationServiceEvent se);

}
 